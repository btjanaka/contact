// CSS imports (necessary because Webpack uses single entry point).
import "../css/index.scss";

// Placeholder until we get real code that needs vendor.js.
Array.from(document.getElementsByTagName("p")).forEach((p) => {});
